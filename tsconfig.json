{
  "compilerOptions": {
    // Allows TS to see the Vitest globals
    "types": ["vitest/globals", "@testing-library/jest-dom"],
    "paths": {
      "corvu": ["./packages/corvu"]
    }
  },
  "exclude": ["node_modules"]
}
